RJHs dotfiles, as a bare git repository.

Instructions for use:

`
alias config="/usr/bin/git --git-dir=$HOME/.rjh.git/ --work-tree=$HOME"
echo ".rjh.git" >> .gitignore
git clone --bare https://www.github.com/rhunt04/dots $HOME/.rjh
config checkout
config config --local status.showUntrackedFiles no
`
